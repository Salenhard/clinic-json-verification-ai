services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data       # persist SQLite file
    env_file:
      - .env
    environment:
      DB_PATH: /app/data/tasks.db
    restart: unless-stopped
